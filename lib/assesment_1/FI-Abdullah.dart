import 'package:flutter/material.dart';

class FIAbdullah extends StatelessWidget {
  const FIAbdullah({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Made by Abdullah"),
      ),
      body: const Center(
        child: Text("Hello to all I am Abdullah Abbasi"),
      ),
    );
  }
}
